# JMagick

There's a problem importing classes from JMagick in boot, but not in leiningen or clj via tools deps.

In each directory `boot`, `lein`, and `clj` run `./build.sh` followed by
`./run.sh` to see the output of running the `magick_script`
